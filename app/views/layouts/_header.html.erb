<nav class="navbar is-danger" role="navigation" aria-label="main navigation">
  <div class="navbar-brand">
    <div class="navbar-item" >
      Winery
    </div>

    <a role="button" class="navbar-burger burger" aria-label="menu" aria-expanded="false"
      data-target="header">
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
    </a>
  </div>

  <div id="header" class="navbar-menu">
    <div class="navbar-start"></div>
    <div class="navbar-end">
        <% if user_signed_in? %>
          <div class="navbar-item has-dropdown is-hoverable">
            <a class="navbar-link">
              <%= current_user.name %>さん
            </a>
            <div class="navbar-dropdown">
              <%= link_to mypage_index_path, 'class': 'navbar-item' do %>
                My Page
              <% end %>
              <%= link_to mypage_index_path, 'class': 'navbar-item' do %>
                Profile
              <% end %>
              <hr class="navbar-divider">
              <%= link_to destroy_user_session_path, method: :delete, 'class': 'navbar-item' do %>
                Logout
              <% end %>
            </div>
          </div>
        <% else %>
          <div class="navbar-item">
            <div class="buttons">
              <%= link_to new_user_registration_path, 'class': 'button is-warning is-light' do %>
                Sign up
              <% end %>
              <%= link_to new_user_session_path, 'class': 'button is-danger is-light' do %>
                Log in
              <% end %>
            </div>
          </div>
        <% end %>

    </div>
  </div>
</nav>